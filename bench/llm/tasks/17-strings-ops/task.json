{
  "kind": "generation",
  "source_checks": {
    "vol": [
      "@std/strings",
      "strings\\.trim\\s*\\(",
      "strings\\.(split|join)\\s*\\(",
      "strings\\.has\\s*\\(",
      "strings\\.replace\\s*\\("
    ],
    "python": [
      "\\.strip\\s*\\(",
      "\\.split\\s*\\(|\\.join\\s*\\(",
      "\\bin\\b",
      "\\.replace\\s*\\("
    ]
  }
}
