# Task 08-strings-assert — repair

Repair the following {{LANG}} program. Return the complete corrected source,
not a diff. Preserve its intent: compute the length of `hello`, duplicate the
word, print both values, and fail an assertion-style check unless the length is
five.

## Broken source

```text
{{STARTER}}
```

## Expected stdout

```text
5
hellohello
```

## Constraints

- Correct the program rather than replacing it with hard-coded output
- Retain an assertion or explicit panic-style invariant check
- Single source file; no network, files, arguments, or stdin
