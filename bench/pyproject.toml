[project]
name = "vol-bench"
version = "0.1.0"
description = "VOL static source token density benchmark (VOL vs Go, Rust, Zig)"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "tiktoken>=0.7",
]
