// @std/fs and @std/path (SF-3.1). Text files + path helpers.

import "@std/fs"
import "@std/path"
import "@std/env"

root := env.get("TMPDIR") ?? "/tmp"
file := path.join(root, "vol_sf3_note.txt")
fs.write(file, "hello from fs")?
print fs.read(file)?
print fs.exists(file)
print path.base(file)
print path.ext(file)
