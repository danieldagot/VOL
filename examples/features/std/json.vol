// @std/json (SF-3.1): json.parse/dump → Result; objects → dict; null → none.

import "@std/json"

print json.dump(dict { host: "example.com" })?
v := json.parse("{\"a\":1,\"b\":null}")?
print v["a"]
print v["b"]
