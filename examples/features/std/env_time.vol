// @std/env and @std/time (SF-3.1). Defaults via Option ?? — no get_or.

import "@std/env"
import "@std/time"

port := env.get("PORT") ?? "8080"
print "port:", port
print time.format(0, "2006-01-02")
