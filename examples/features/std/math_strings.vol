// @std/math and @std/strings (SF-3.1).

import "@std/math"
import "@std/strings"

print math.abs(-40)?
print math.clamp(12, 0, 10)?
print strings.trim("  vol  ")
print strings.join(strings.split("a,b,c", ","), "-")
print strings.has("vocabulary", "cab")
