// @std/math and @std/strings (SF-3).

import "@std/math"
import "@std/strings"

print abs(-40)?
print clamp(12, 0, 10)?
print trim("  vol  ")
print join(split("a,b,c", ","), "-")
print has("vocabulary", "cab")
