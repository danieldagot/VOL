// @std/process (SF-3.1): process.run(argv) → Result with status/stdout/stderr.

import "@std/process"
import "@std/strings"

p := process.run(["echo", "vol"])?
print p.status
print strings.trim(p.stdout)
